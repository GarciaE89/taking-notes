// fs and path dependencies 
const fs = require('fs');
const path = require('path');

function findNotebyId()

function createNote()

function validateNotes()

module.exports = {
    findNotebyId,
    createNote,
    validateNotes
};